name: spg-lcda
channels:
  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge
  - nodefaults
dependencies:
  - python=3.11
  - pip=24.3
  - git
  - ffmpeg
  - libgl

# GPU PyTorch is deliberately installed by scripts/setup_server.sh so that
# its CUDA wheel can be matched to the server driver instead of guessed here.
